#pragma once

namespace RoninEngine {
// default square pixel/100
constexpr float squarePerPixels = 100;
constexpr float spaceDiv = squarePerPixels / 2.f;
constexpr float maxWorldScalar = 10000;
}  // namespace RoninEngine
